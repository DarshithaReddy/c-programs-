#include<stdio.h>
main()
{
	int n,i,c=0;
    printf("Enter the number:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(n%i==0)	{
		c++;  }
	}
	
	printf("\n%d",c);
	printf("( ");
    for (i = 1; i <= n; i++) {
        if (n % i == 0) {
            printf("%d ", i);
        }
    }
    printf(")");
}
